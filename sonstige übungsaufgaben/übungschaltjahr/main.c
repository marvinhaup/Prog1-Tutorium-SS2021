#include <stdio.h>
#include <stdlib.h>

int main()
{

    int jahreszahl;
    printf("Geben Sie eine Jahreszahl ein:");
    scanf("%d", &jahreszahl);

    (jahreszahl % 4) == 0 ? printf("Ist ein Schaltjahr") : printf("ist kein Schaltjahr");
        1 ? 2 :3


    return 0;
}
